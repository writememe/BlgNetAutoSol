{% for interface in interfaces %}
interfaces {
    {{ interface.name }} {
        description {{ interface.description }};
	unit 0 {
            family inet {
                address {{ interface.ip }};
            }
        }    
    } 
}

{% endfor %}
